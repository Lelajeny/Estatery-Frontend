{
  "name": "estatery-apps",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix admin",
    "dev:admin": "npm run dev --prefix admin",
    "dev:customer": "npm run dev --prefix customer -- -p 3001"
  }
}
